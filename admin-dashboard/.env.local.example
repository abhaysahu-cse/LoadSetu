# LoadSetu Admin Dashboard — Environment Variables
# Copy this file to .env.local and fill in your values

# FastAPI ML Engine URL (admin endpoints: trucks/live, load-events, matches, force-match)
FASTAPI_URL=http://localhost:8000

# Spring Boot backend URL (auth, loads, bookings, payments)
SPRING_BOOT_URL=http://localhost:8080
